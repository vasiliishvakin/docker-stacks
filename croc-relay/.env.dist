# === Croc relay ===

# Shared password for croc clients
PASS=change_me_strong_password

# Host that relay binds to inside container
HOST=0.0.0.0

# Ports used by croc relay (comma-separated)
PORTS=9009,9010,9011

# Exposed ports range (must match PORTS)
PORTS_START=9009
PORTS_END=9011

# Docker restart policy
RESTART_POLICY=unless-stopped

# Docker network name (must exist and be external)
NETWORK_NAME=shared

#