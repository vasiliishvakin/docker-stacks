# Docker Builder Environment Configuration
# Copy this file to .env and configure with your values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# SSH public key for authentication (optional, can be added later via docker exec)
# Example: ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIExampleKey... user@hostname
SSH_PUBLIC_KEY=

# ============================================================================
# NETWORK CONFIGURATION
# ============================================================================

# Network mode - uses Tailscale container's network namespace by default
# This makes docker-builder accessible via Tailscale network
# To use standalone mode: NETWORK_MODE=bridge (and uncomment networks section in compose.yaml)
NETWORK_MODE=container:tailscale

# ============================================================================
# CONTAINER CONFIGURATION
# ============================================================================

# Restart policy for the container
# Options: no, always, on-failure, unless-stopped
RESTART_POLICY=unless-stopped

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Optional: Workspace mount (uncomment and add to compose.yaml volumes section)
# WORKSPACE=/path/to/your/project
