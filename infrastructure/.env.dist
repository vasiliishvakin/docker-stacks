# Docker Infrastructure Configuration
# Copy: cp .env.dist .env

# Network Configuration
NETWORK_NAME=shared
NETWORK_SUBNET=172.24.0

# Network IP allocation (automatically configured):
#   Static:  .1-.127    (manual assignment)
#   DHCP:    .128-.254  (automatic assignment)
#   Gateway: .1

# Volume Management
# Format: Space-separated list of volumes to create
# Run 'task list-volumes' to see what will be created
# Examples:
#   VOLUMES=portainer_data traefik_certs prometheus_data
#   VOLUMES=""  # No volumes (empty string also works)

VOLUMES=portainer_data traefik_certs prometheus_data grafana_data alertmanager_data mysql_data

# Common volumes (add to VOLUMES line above as needed):
# postgres_data mysql_data redis_data mongodb_data rabbitmq_data
# minio_data loki_data nginx_config nextcloud_data
#
# To remove a volume manually: docker volume rm <volume_name>
